## Valid Children {/*valid-children*/}

The `<select>` element can contain:
- `<option>`
- `<optgroup>`
- `<hr>`
- `<script>`
- `<template>`
- Text nodes

This follows the [HTML specification for parsing in select context](https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect).